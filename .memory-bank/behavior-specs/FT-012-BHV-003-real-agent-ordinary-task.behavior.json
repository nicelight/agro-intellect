{
  "id": "FT-012-BHV-003",
  "feature_id": "FT-012",
  "title": "Provider-neutral Task and Follow-up Agent can create only a matched ordinary task",
  "given": {
    "plant": "active and currently authorized",
    "provider": "one explicit test-only task_follow_up fake/spy executor; production remains unbound and fail closed",
    "context": "strict PostgreSQL task, outcome, and evidence records only",
    "run_identity": "one TaskFollowUp command run_id with its canonical command fingerprint"
  },
  "when": {
    "success_flow": "typed model proposal passes the post-model current guard, commits one runtime envelope_handed_off disposition, becomes one pending task_request envelope, and receives the matching project classification",
    "denial_flow": "the same stage instead observes archive or revoke and commits publication_denied before any envelope or classification"
  },
  "then": {
    "created_task_kind": "the same check|measurement|follow_up kind",
    "created_task_count": 1,
    "same_run_retry": "cannot allocate another message; an identical denied retry returns the stored denial and conflicting input fails closed",
    "restore_and_concurrency": "restore cannot make the denied run operative, concurrent same-run terminal writes cannot contradict classified dispatch, and reevaluation requires a new command/run/message",
    "action_task_created": false,
    "approval_or_completion_effect": false,
    "fake_fallback_or_silent_acceptance": false
  }
}
