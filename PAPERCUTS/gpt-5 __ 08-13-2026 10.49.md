- A canonical spec-path uniqueness check counted the same path twice inside one
  Markdown link (label and target), falsely reporting duplicate registry rows.
  Parse spec-index table rows or link targets instead of counting raw substrings.
- A spec-index purity check mixed the five-column Spec Registry with the
  separate four-column Planned Specs table. Scope structural checks between the
  owning section headings.
